3.6.8 (default, Aug  7 2019, 17:28:10) 
[GCC 4.8.5 20150623 (Red Hat 4.8.5-39)]
['/home/jgreulich/scripts/python', '/usr/lib64/python36.zip', '/usr/lib64/python3.6', '/usr/lib64/python3.6/lib-dynload', '/home/jgreulich/.local/lib/python3.6/site-packages', '/usr/lib64/python3.6/site-packages', '/usr/lib/python3.6/site-packages']
----------------------------------------------------
--------------- stages --------------------------
----------------------------------------------------
['sanity', 'validation', 'acceptance', 'compliance', 'deployment']
----------------------------------------------------
--------------- variables --------------------------
----------------------------------------------------
{'PUPPET_VERSION': 'UNDEFINED', 'BUNDLER_VERSION': '1.17.1', 'GEM_HOME': '.vendor/gem_install', 'BUNDLE_CACHE_PATH': '.vendor/bundle', 'BUNDLE_PATH': '.vendor/bundle', 'BUNDLE_BIN': '.vendor/gem_install/bin', 'BUNDLE_NO_PRUNE': 'true'}
----------------------------------------------------
--------------- .setup_bundler_env --------------------------
----------------------------------------------------
{'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }']}
----------------------------------------------------
--------------- .only_with_SIMP_FULL_MATRIX --------------------------
----------------------------------------------------
{'only': {'variables': ['$SIMP_FULL_MATRIX == "yes"']}}
----------------------------------------------------
--------------- .pup_4 --------------------------
----------------------------------------------------
{'image': 'ruby:2.1', 'variables': {'PUPPET_VERSION': '~> 4.0', 'MATRIX_RUBY_VERSION': '2.1'}}
{'PUPPET_VERSION': '~> 4.0', 'MATRIX_RUBY_VERSION': '2.1'}
----------------------------------------------------
--------------- .pup_4_10 --------------------------
----------------------------------------------------
{'image': 'ruby:2.1', 'variables': {'PUPPET_VERSION': '~> 4.10.4', 'MATRIX_RUBY_VERSION': '2.1'}}
{'PUPPET_VERSION': '~> 4.10.4', 'MATRIX_RUBY_VERSION': '2.1'}
----------------------------------------------------
--------------- .pup_5 --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '~> 5.0', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}}
{'PUPPET_VERSION': '~> 5.0', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
----------------------------------------------------
--------------- .pup_5_5_7 --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}}
{'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
----------------------------------------------------
--------------- .pup_6 --------------------------
----------------------------------------------------
{'allow_failure': True, 'image': 'ruby:2.5', 'variables': {'PUPPET_VERSION': '~> 6.0', 'BEAKER_PUPPET_COLLECTION': 'puppet6', 'MATRIX_RUBY_VERSION': '2.5'}}
{'PUPPET_VERSION': '~> 6.0', 'BEAKER_PUPPET_COLLECTION': 'puppet6', 'MATRIX_RUBY_VERSION': '2.5'}
----------------------------------------------------
--------------- .lint_tests --------------------------
----------------------------------------------------
{'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'validation', 'tags': ['docker'], 'script': ['bundle exec rake syntax', 'bundle exec rake lint', 'bundle exec rake metadata_lint']}
parsing scripts
----------------------------------------------------
--------------- .unit_tests --------------------------
----------------------------------------------------
{'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'validation', 'tags': ['docker'], 'script': ['bundle exec rake spec']}
parsing scripts
----------------------------------------------------
--------------- .acceptance_base --------------------------
----------------------------------------------------
{'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'acceptance', 'tags': ['beaker']}
----------------------------------------------------
--------------- .compliance_base --------------------------
----------------------------------------------------
{'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'compliance', 'tags': ['beaker']}
----------------------------------------------------
--------------- sanity_checks --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '~> 5.0', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'sanity', 'tags': ['docker'], 'script': ['if `hash apt-get`; then apt-get update; fi', 'if `hash apt-get`; then apt-get install -y rpm; fi', 'bundle exec rake check:dot_underscore', 'bundle exec rake check:test_file', 'bundle exec rake pkg:check_version', 'bundle exec rake pkg:compare_latest_tag', 'bundle exec rake pkg:create_tag_changelog', 'bundle exec puppet module build']}
{'PUPPET_VERSION': '~> 5.0', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
parsing scripts
----------------------------------------------------
--------------- pup4-lint --------------------------
----------------------------------------------------
{'image': 'ruby:2.1', 'variables': {'PUPPET_VERSION': '~> 4.0', 'MATRIX_RUBY_VERSION': '2.1'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'validation', 'tags': ['docker'], 'script': ['bundle exec rake syntax', 'bundle exec rake lint', 'bundle exec rake metadata_lint']}
{'PUPPET_VERSION': '~> 4.0', 'MATRIX_RUBY_VERSION': '2.1'}
parsing scripts
----------------------------------------------------
--------------- pup5-lint --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '~> 5.0', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'validation', 'tags': ['docker'], 'script': ['bundle exec rake syntax', 'bundle exec rake lint', 'bundle exec rake metadata_lint']}
{'PUPPET_VERSION': '~> 5.0', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
parsing scripts
----------------------------------------------------
--------------- pup6-lint --------------------------
----------------------------------------------------
{'allow_failure': True, 'image': 'ruby:2.5', 'variables': {'PUPPET_VERSION': '~> 6.0', 'BEAKER_PUPPET_COLLECTION': 'puppet6', 'MATRIX_RUBY_VERSION': '2.5'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'validation', 'tags': ['docker'], 'script': ['bundle exec rake syntax', 'bundle exec rake lint', 'bundle exec rake metadata_lint']}
{'PUPPET_VERSION': '~> 6.0', 'BEAKER_PUPPET_COLLECTION': 'puppet6', 'MATRIX_RUBY_VERSION': '2.5'}
parsing scripts
----------------------------------------------------
--------------- pup5-unit --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '~> 5.0', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'validation', 'tags': ['docker'], 'script': ['bundle exec rake spec']}
{'PUPPET_VERSION': '~> 5.0', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
parsing scripts
----------------------------------------------------
--------------- pup5.5.7-unit --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'validation', 'tags': ['docker'], 'script': ['bundle exec rake spec']}
{'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
parsing scripts
----------------------------------------------------
--------------- pup4.10-unit --------------------------
----------------------------------------------------
{'image': 'ruby:2.1', 'variables': {'PUPPET_VERSION': '~> 4.10.4', 'MATRIX_RUBY_VERSION': '2.1'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'validation', 'tags': ['docker'], 'script': ['bundle exec rake spec']}
{'PUPPET_VERSION': '~> 4.10.4', 'MATRIX_RUBY_VERSION': '2.1'}
parsing scripts
----------------------------------------------------
--------------- pup6-unit --------------------------
----------------------------------------------------
{'allow_failure': True, 'image': 'ruby:2.5', 'variables': {'PUPPET_VERSION': '~> 6.0', 'BEAKER_PUPPET_COLLECTION': 'puppet6', 'MATRIX_RUBY_VERSION': '2.5'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'validation', 'tags': ['docker'], 'script': ['bundle exec rake spec']}
{'PUPPET_VERSION': '~> 6.0', 'BEAKER_PUPPET_COLLECTION': 'puppet6', 'MATRIX_RUBY_VERSION': '2.5'}
parsing scripts
----------------------------------------------------
--------------- pup4.10 --------------------------
----------------------------------------------------
{'image': 'ruby:2.1', 'variables': {'PUPPET_VERSION': '~> 4.10.4', 'MATRIX_RUBY_VERSION': '2.1'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'acceptance', 'tags': ['beaker'], 'script': ['bundle exec rake beaker:suites[default]']}
{'PUPPET_VERSION': '~> 4.10.4', 'MATRIX_RUBY_VERSION': '2.1'}
parsing scripts
----------------------------------------------------
--------------- pup4.10-fips --------------------------
----------------------------------------------------
{'image': 'ruby:2.1', 'variables': {'PUPPET_VERSION': '~> 4.10.4', 'MATRIX_RUBY_VERSION': '2.1'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'acceptance', 'tags': ['beaker'], 'only': {'variables': ['$SIMP_FULL_MATRIX == "yes"']}, 'script': ['BEAKER_fips=yes bundle exec rake beaker:suites[default]']}
{'PUPPET_VERSION': '~> 4.10.4', 'MATRIX_RUBY_VERSION': '2.1'}
parsing scripts
----------------------------------------------------
--------------- pup5.5.7 --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'acceptance', 'tags': ['beaker'], 'script': ['bundle exec rake beaker:suites[default]']}
{'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
parsing scripts
----------------------------------------------------
--------------- pup5.5.7-fips --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'acceptance', 'tags': ['beaker'], 'script': ['BEAKER_fips=yes bundle exec rake beaker:suites[default]']}
{'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
parsing scripts
----------------------------------------------------
--------------- pup5.5.7-oel --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'acceptance', 'tags': ['beaker'], 'script': ['bundle exec rake beaker:suites[default,oel]']}
{'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
parsing scripts
----------------------------------------------------
--------------- pup4.10-stunnel --------------------------
----------------------------------------------------
{'image': 'ruby:2.1', 'variables': {'PUPPET_VERSION': '~> 4.10.4', 'MATRIX_RUBY_VERSION': '2.1'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'acceptance', 'tags': ['beaker'], 'only': {'variables': ['$SIMP_FULL_MATRIX == "yes"']}, 'script': ['bundle exec rake beaker:suites[stunnel]']}
{'PUPPET_VERSION': '~> 4.10.4', 'MATRIX_RUBY_VERSION': '2.1'}
parsing scripts
----------------------------------------------------
--------------- pup4.10-fips-stunnel --------------------------
----------------------------------------------------
{'image': 'ruby:2.1', 'variables': {'PUPPET_VERSION': '~> 4.10.4', 'MATRIX_RUBY_VERSION': '2.1'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'acceptance', 'tags': ['beaker'], 'only': {'variables': ['$SIMP_FULL_MATRIX == "yes"']}, 'script': ['BEAKER_fips=yes bundle exec rake beaker:suites[stunnel]']}
{'PUPPET_VERSION': '~> 4.10.4', 'MATRIX_RUBY_VERSION': '2.1'}
parsing scripts
----------------------------------------------------
--------------- pup5.5.7-stunnel --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'acceptance', 'tags': ['beaker'], 'script': ['bundle exec rake beaker:suites[stunnel]']}
{'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
parsing scripts
----------------------------------------------------
--------------- pup5.5.7-fips-stunnel --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'acceptance', 'tags': ['beaker'], 'script': ['BEAKER_fips=yes bundle exec rake beaker:suites[stunnel]']}
{'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
parsing scripts
----------------------------------------------------
--------------- pup5.5.7-oel-stunnel --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'acceptance', 'tags': ['beaker'], 'script': ['bundle exec rake beaker:suites[stunnel,oel]']}
{'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
parsing scripts
----------------------------------------------------
--------------- pup5.5.7-oel-fips-stunnel --------------------------
----------------------------------------------------
{'image': 'ruby:2.4', 'variables': {'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}, 'cache': {'untracked': True, 'key': '${CI_PROJECT_NAMESPACE}_ruby-${MATRIX_RUBY_VERSION}_bundler', 'paths': ['.vendor']}, 'before_script': ['ruby -e "puts %(\\n\\n), %q(=)*80, %(\\nSIMP-relevant Environment Variables:\\n\\n#{e=ENV.keys.grep(/^PUPPET|^SIMP|^BEAKER|MATRIX/); pad=e.map{|x| x.size}.max+1; e.map{|v| %(    * #{%(#{v}:).ljust(pad)} #{39.chr + ENV[v] + 39.chr}\\n)}.join}\\n),  %q(=)*80, %(\\n\\n)"', 'declare GEM_BUNDLER_VER=(-v "~> ${BUNDLER_VERSION:-1.17.1}")', 'declare GEM_INSTALL_CMD=(gem install --no-document)', 'declare BUNDLER_INSTALL_CMD=(bundle install --no-binstubs --jobs $(nproc) "${FLAGS[@]}")', 'mkdir -p ${GEM_HOME} ${BUNDLER_BIN}', 'gem list -ie "${GEM_BUNDLER_VER[@]}" --silent bundler || "${GEM_INSTALL_CMD[@]}" --local "${GEM_BUNDLER_VER[@]}" bundler || "${GEM_INSTALL_CMD[@]}" "${GEM_BUNDLER_VER[@]}" bundler', 'rm -rf pkg/ || :', 'bundle check || rm -f Gemfile.lock && ("${BUNDLER_INSTALL_CMD[@]}" --local || "${BUNDLER_INSTALL_CMD[@]}" || bundle pristine ||  "${BUNDLER_INSTALL_CMD[@]}") || { echo "PIPELINE: Bundler could not install everything (see log output above)" && exit 99 ; }'], 'stage': 'acceptance', 'tags': ['beaker'], 'only': {'variables': ['$SIMP_FULL_MATRIX == "yes"']}, 'script': ['BEAKER_fips=yes bundle exec rake beaker:suites[stunnel,oel]']}
{'PUPPET_VERSION': '5.5.7', 'BEAKER_PUPPET_COLLECTION': 'puppet5', 'MATRIX_RUBY_VERSION': '2.4'}
parsing scripts
['~> 6.0']
['~> 5.0', '~> 4.10.4', '5.5.7', '~> 4.0']
